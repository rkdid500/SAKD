# 싹딜(SAKD) 프로젝트 이어서 작업

## 0. 시작 전 필수
회사 컴퓨터의 로컬 저장소가 최신 상태인지 먼저 확인하고 반영해줘.
```
git fetch --all
git pull --ff-only
```
저장소: https://github.com/rkdid500/SAKD (branch: master)

## 1. 지금까지 완료된 작업 (최근 커밋 순)
- `0ee6f62` — 카테고리 단계적 오픈을 위한 사이트 스냅샷 3종 생성
  - `인터넷/` : 메인페이지(sakd.html) + 인터넷.html만 남기고 카테고리 탭·상품 카드에서 스마트폰/보험/상조 제거
  - `인터넷스마트폰/` : 메인페이지 + 인터넷.html + phone.html, 탭·카드에서 보험/상조만 제거
  - `AII/` : 현재 전체 사이트를 원본 그대로 복사 (5개 페이지 전부 포함)
  - 세 폴더 모두 css/js/images는 동일하게 복사됨, 원본 루트 파일은 건드리지 않음
- `6ca548c` — "왜 싹딜을 선택해야 할까요" 섹션 아이콘/차트 폴리싱, 히어로 비주얼 사이즈/배경 조정, `cat-intro` 카드 4개 박스 크기 불일치 수정
  - 아이콘을 Microsoft Fluent 3D emoji PNG로 교체 (`images/emoji-*-3d.png`)
  - 비교 막대그래프에 스크롤 진입 시 자라나는 애니메이션 추가
  - 히어로 섹션 배경색을 삽입된 WebP 애니메이션 배경색과 정확히 매칭, 비주얼 크기 약 1.1배 확대
  - `cat-intro__card`에 고정 `aspect-ratio` + `object-fit:cover` 적용해 스마트폰 카드가 유독 커보이던 문제 해결 (`css/style.css`)
- `896b8dc` 이전 — PC/모바일 히어로 섹션 전체 재구축(WebP intro→loop 전환 애니메이션), "왜 싹딜을 선택해야 할까요" 섹션 최초 구현, 비교 상담 누적 수치 조정 등

## 2. 프로젝트 구조 참고
- 정적 HTML/CSS/JS, 빌드 도구 없음
- 루트: `sakd.html`(메인), `인터넷.html`, `phone.html`, `insurance.html`, `funeral.html`
- 공통: `css/style.css`, `js/main.js` / 인터넷 페이지 전용: `css/internet.css`, `js/internet.js`
- 로컬 프리뷰 서버: `.claude/launch.json`에 등록된 PowerShell 정적 서버, 포트 8934 (Python/Node 미설치 환경이라 이 방식 사용)
- GitHub Pages 배포 주소: https://rkdid500.github.io/SAKD/sakd.html

## 3. 작업 스타일 (반드시 지켜야 할 것)
- 사용자가 스크린샷을 보내며 아주 구체적인 디테일(여백, 색상, 크기, 애니메이션 등) 수정을 요청하는 방식으로 진행됨. "PC도/모바일도 동일하게" 요청이 매우 잦으니 항상 두 브레이크포인트 모두 확인할 것
- **커밋/푸시는 사용자가 "커밋하고 푸시해줘"라고 명시적으로 말할 때만 수행**
- 커밋 메시지 마지막 줄: `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`
- 시각적 변경 후에는 반드시 브라우저(localhost:8934)로 PC(1300px 이상)와 모바일(375px) 두 사이즈 모두 스크린샷으로 확인 후 보고
- 회사 컴퓨터와 개인 노트북 두 대를 오가며 작업 중이므로, 세션 시작 시 항상 git pull로 동기화 상태부터 맞출 것

## 4. 다음에 이어서 할 일
- 명시적으로 남은 작업은 없음 — 사용자가 다음에 스크린샷/요청을 주는 대로 세부 조정 진행
- `인터넷/`, `인터넷스마트폰/` 폴더는 보험/상조를 나중에 추가하기 위한 단계적 배포용 스냅샷이므로, 앞으로 루트 파일을 수정할 때 이 폴더들에도 동일하게 반영해야 하는지 사용자에게 먼저 확인할 것 (아직 자동 동기화 로직 없음, 수동 복사 방식)
