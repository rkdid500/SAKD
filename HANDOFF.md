# SAKD 프로젝트 작업 이어가기

저장소: `https://github.com/rkdid500/SAKD` (branch: master), GitHub Pages 배포. 로컬 경로는 `Desktop/SAKD`.

## 작업 범위 (중요, 반드시 지켜줘)

지금은 `인터넷스마트폰/` 폴더에서만 작업 중이야. 루트나 `인터넷/`, `AII/` 등 다른 스냅샷 폴더는 건드리지 마. (사이트가 카테고리 단계적 오픈 구조라서 스냅샷 폴더별로 분리돼 있음)

## 작업 방식 규칙

- 커밋·푸시는 내가 "커밋하고 푸시해줘"라고 명시적으로 말할 때만 해줘.
- 커밋 메시지 끝에 `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` 붙여줘.
- 변경 전후로 브라우저에서 데스크톱(~1280px)·모바일(375px) 둘 다 확인해줘 (PowerShell 정적 서버, 포트 8934 — Python 없음, Node는 있음).

## 지금까지 `인터넷스마트폰/phone.html` 작업 내역 (스마트폰 비교 페이지 완전 재구성)

1. 처음엔 인터넷 페이지(`인터넷.html`)와 거의 동일한 구성(히어로+파인더/신뢰배지/3단계/CTA)으로 만들었다가 반려됨 — "인터넷 페이지랑 똑같이 만들면 안 된다"는 피드백.
2. 벤치마킹 3사이트(아정당 ajd.co.kr, 폰슐랭샵 phonechelin.shop, 투게더몰 uplustogethermall.com) 분석해서 요소를 조합, 완전히 다른 구성으로 재작업:
   - **프로모션 배너 캐러셀** — 실제 배너 이미지(`images/아이폰배너.png`, `images/갤럭시배너.png`) + 3사 비교 텍스트 슬라이드. 자동재생 5초, 화살표/점 네비게이션. 배너는 풀블리드가 아니라 하단 콘텐츠와 같은 폭(컨테이너)에 맞춰 둥근 모서리 박스로 들어감.
   - **진입 경로 3카드** — 온라인 셀프비교 / 전화 상담 / 매장 찾기
   - **실제 개통 후기** — 데스크톱은 큰 사진 카드 3개, 모바일(~768px)은 썸네일+텍스트 컴팩트 2열 카드로 전환 (`review__grid` vs `review__list` 토글)
   - **기기 가격 카드 그리드** — 전체/아이폰/갤럭시 필터 탭. 실제 최신 기종 라인업(출시순): 아이폰 18 프로맥스→18 프로→17 프로맥스→17 프로→17, 갤럭시 Z폴드8 울트라→Z폴드8→Z플립8→S26 울트라→S26→Z폴드7→Z플립7. 각 카드는 출고가→공시지원금 할인→싹딜 추가할인→기기값 구조. **자급제/알뜰폰은 취급 안 함** (3사 통신사만 운영) — 관련 문구 전부 제거됨. 모바일에서는 2열 컴팩트 카드 4개만 먼저 보여주고 "더보기" 버튼으로 나머지 펼침.
   - **가입 혜택 그리드** — 보험무료가입/사은품/경품추첨/후기혜택 4카드
   - **FAQ**, 푸터
3. 버그 2건 수정:
   - 브랜드 필터 탭 클릭해도 카드가 안 사라지던 문제 — `.device-card{display:flex}`가 `[hidden]` 속성보다 CSS 우선순위에서 이겨서 발생. `.device-card[hidden]{display:none}` 명시 규칙 추가로 해결.
   - 배너 캐러셀에서 두 번째(갤럭시)/세 번째 슬라이드가 안 보이던 문제 — `overflow:hidden`을 이동하는 트랙 요소 자체에 걸어놔서 클리핑 영역이 같이 움직여 항상 잘려 있었음. 별도의 고정 래퍼(`.phone-banner__frame`)로 클리핑을 분리해서 해결.
4. 최신 커밋: `00582ba` (배너 이미지 적용 + 캐러셀 버그 수정)까지 푸시 완료.

## 참고 (미처리 상태)

루트 `images/` 폴더에도 배너 이미지 사본이 업로드돼 있는데, 작업 범위 밖이라 커밋하지 않고 남겨둔 상태야 (신경 안 써도 됨).

## 다음 작업

따로 지시한 다음 작업은 없음 — 디자인 디테일 수정 요청 있으면 스크린샷과 함께 알려줘.
